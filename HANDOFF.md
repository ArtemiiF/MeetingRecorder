# HANDOFF — состояние на 2026-07-22

Electron-приложение записи встреч: mic+system (AudioTee) / импорт → `backend.py` (+`backend_core.py` — чистая логика) JSON-stdout: mono/VAD → MLX Whisper → коррекция → pyannote → LLM-сводка (LM Studio `:1234`) → `.md` в Obsidian. **Шесть** видов в сайдбаре: 🎙 Запись · 📚 История · 🗂 PARA · 📖 Словарь · 📝 Промпты · 🗑 Корзина. Конвенции агентам — в корневом `CLAUDE.md` (с #32; § Артефакты — с #35).

## Сессия 2026-07-21/22 — компакт-рейл Истории (вариант B, v1.4.12) + батч хвостов (v1.4.13, PR #38–#39)

Статус: **всё смержено и выпущено** (оба PR смержены владельцем, релизы v1.4.12 и v1.4.13 опубликованы). Сессия из `~/SupersetWorkspace` (push-гейт — bypass-ритуал, схема прежняя; на батч хвостов владелец дал blanket-окно «добей хвосты, меня не отвлекай»).

**Проверено в этом прогоне (команда → вывод, финальная сверка при хендоффе):**
- `git log -1 --oneline` (main после ff) → `e449a94 chore: батч хвостов … (v1.4.13) (#39)`; в логе перед ним `cf7dd93` (#38, v1.4.12).
- Тесты на main e449a94: `npm run test:js` → **JS 624/624 (fail 0)**; `pytest -q` → **PY 451 passed**.
- `gh pr view 39` → MERGED; `gh pr checks 39` → js/py/e2e-boot/mutation/mutation-js все pass.
- `gh release view v1.4.13` → тег есть, draft:false; v1.4.12 → published 2026-07-21T09:36:14Z.
- Мерженные локальные ветки удалены (`git branch -d` ok: feat/history-compact-rail, chore/tails-batch).

**PR #38 (`cf7dd93`, v1.4.12, СМЕРЖЕН) — компакт-рейл Истории.** Триггер: владелец показал скриншот («нечитаемое говно»). Диагноз: 4 визуальных этажа на запись, красная кнопка «В корзину» на каждой карточке, шумовые «1 обработка»/«· v1 (latest)». Флоу: 2 концепт-карточки в Claude Design (`screens/history-compact-a/b.html`, манифест правлен вручную — ловушка прежняя) → владелец выбрал **B** («карточка, но тише») → developer → критик (approve-with-nits, ниты закрыты вторым коммитом) → скриншот-прогон (урок v1.4.10 соблюдён) → push по разрешению → мерж. Итог: шапка группы = одна строка (каретка+🎙+пилюля только при ≥2+время+тихий 🗑 hover-danger), мета-строка и `.rail-actions` сняты, «(latest)» убран везде, одиночная заметка шаблона без «· v1», сирота в одну строку; шапка теперь `div role="button"` (внутри настоящая 🗑-`<button>`, вложенные button невалидны) с Enter+Space+preventDefault и focus-visible; orphan-бейдж «без обработок» и его CSS снесены как мёртвые.

**PR #39 (`e449a94`, v1.4.13, СМЕРЖЕН) — батч хвостов, 12 коммитов** (флоу: developer×2 → критик → ниты оркестратором → push в blanket-окне):
- 9 код-хвостов: ru/en-пин `Pipeline.summarize()`; para-classify containment root/folders (+re-check после join); обратный контракт-лок emit-call-sites ⊆ events.json (в backend.py — формулировка хвоста была про main.js, там emit() нет, агент перевывел); сироты `glossaryCategories`; `cleanupStrandedBackendEnvOld()` (launch + uninstall, только `.old`); sync-reject → `finishPendingItem("failed")`; `_glossary_prompt` DRY; PARA inbox surfacing + in-flight guard; «разобрать все» finally.
- mutmut **82→93.61%** (+79 тестов, zero-import; остаток = эквиваленты/таймауты, разбор в `.claude/agent-reports/developer-tails2.md`).
- **StrykerJS** на mainutil: 85.98%, ~40с, report-only CI-джоба `mutation-js`; `@stryker-mutator/core` только в devDependencies; `.stryker-tmp/` в .gitignore.
- Ниты критика (`fce0230`): скор-комментарий mutation в ci.yml актуализирован, рационале mutation-js из non-schema `_comment` перенесён в комментарий джобы, TODO-заметка про continuePendingBatch-чейнинг, бамп 1.4.13.

**Решения владельца (сессия):** рейл — через Claude Design (вариант C флоу), выбран концепт B; «добей хвосты какие можешь, меня не отвлекай» = blanket-окно на батч (push без пер-раз разрешения, мержи за ним).

**Ловушки (новые):**
- `gh pr checks` exit 8 = pending, не фейл (после лага регистрации).
- Хвосты-формулировки из HANDOFF протухают против кода: 2 из 9 не совпали с реальностью (root/folders не уходят в backend; emit() не в main.js) — developer перевыводил реальную дыру, это норм-паттерн, писать в спеках «verify against code first».
- json.dumps-перезапись stryker.conf.json реформатирует файл — шумовой дифф.

**Следующие шаги:**
1. **Живой смоук так и висит** (не начат, нужны руки владельца): компакт-рейл на реальных данных (hover-🗑, confirm корзины, раскрывашки ≥2), раскладка «Разложить» (one-to-one → «один-на-один с <Имя>», архив → 2026/07), корзина restore/purge, обновление 1.4.11→1.4.13 через автообновлялку.
2. Не-блокер из TODO: continuePendingBatch-чейнинг на sync-reject пути (renderer.js, см. секцию TODO 2026-07-22).
3. Гейт релиза на e2e-boot/mutation — за владельцем (висит 3-ю сессию; скоры 93.6/86 — аргумент за).
4. Зеркало Claude Design (build_screens.py сломан с #27; history-карточки устарели теперь и под компакт-рейл) + ARIA-нюанс шапки рейла (вложенный interactive) — при желании.
5. Прежние: эмодзи-стек, иконка .app, калибровка auto-«Я».

**Открытые вопросы:** гейтить ли релиз на e2e/mutation; судьба зеркала Claude Design; эмодзи/радиусы (висят с #27).

**Быстрый старт (свежая сессия):** `cd ~/MeetingRecorder/app` (снимает push-гейт-цирк) · `git fetch && git merge --ff-only origin/main` (main = e449a94, v1.4.13) · тесты `npm run test:js` + `../venv/bin/python -m pytest tests/ -q` · JS-мутационка `npx stryker run` (~40с), PY — setup.cfg · артефакт-схема: https://claude.ai/code/artifact/c6589f95-071a-424b-b567-a34dcf792265 (обновлена под v1.4.10; компакт-рейл #38 структуру IPC/пайплайна не двигал, карточка history в ней устарела визуально) · первое действие: живой смоук шага 1.

## Сессия 2026-07-19/20 — UX/PARA-батч из надиктовки + фикс-цикл после живого смоука (v1.4.10–v1.4.11, PR #35–#37)

Статус: **всё смержено и выпущено**. Сессия из `~/SupersetWorkspace` (push-гейт — bypass-ритуал по разрешению владельца на каждый push, схема прежняя; маркер одноразовый).

**Проверено в этом прогоне (команда → вывод):**
- `git log -1 --oneline` (main после ff) → `b83496e fix(ui): переполнения после v1.4.10 … (v1.4.11) (#37)`; в логе #35 `1643588` (docs) → #36 `d4090f4` (батч) → #37.
- `npm run test:js` на main b83496e → **JS 613/613** (mainutil 203 + renderer 410); `pytest -q` → **368 passed**.
- `gh release view v1.4.10 / v1.4.11` → оба тега существуют (v1.4.11 — с ассетами релиз-джобы); `gh pr view 35/36/37` → все MERGED.
- Мерженные локальные ветки удалены (`git branch -d` ok).

**Источник батча:** надиктовка `meeting-2026-07-19-184805` («Организация хранения данных и дизайна») → 7 задач. Флоу: analyzer-разведка → концепт-карточки в Claude Design (владелец выбрал «вариант A» оба) → developer одной веткой → критик-гейт (approve-with-nits) → дельта по решениям владельца → push → PR #36 → владелец смержил.

**Что доехало (PR #36, `d4090f4`, v1.4.10):**
- *История*: карточка записи — явные подписанные действия («🗑 В корзину» danger-тинт, hover-заливка; ▶ где действие существовало) вместо безымянного ✕; 🗑 на строках версий; фикс переполнения на узкой ширине (flex-wrap + min-width:0).
- *PARA-навигация*: вертикальный subnav (160px) снесён → горизонтальный сегмент-контрол; дерево Хранилища 38% → 220px; тексту ~2/3 ширины.
- *PARA-раскладка*: `paraDestinationDir()` (lib/mainutil.js, чистый) — archives/areas/resources → `<Кат>/<год>/<месяц>/`; projects one_to_one → `один-на-один с <Имя>`; mission_daily → `миссии/<год>. миссия <название>`; other → `<Projects>/<проект>/`. Санитайзер per-segment (`/ \ :` + ведущие точки — дот-папки невидимы для reconcile), containment re-check финального destDir (дыра из analyzer-находки закрыта). `cmd_classify` возвращает kind/person/mission (один LLM-вызов, жёсткий код-гейт по паттерну PR #13) + **ru/en-пин промпта** (зеркало generate_title).
- *Выпилено целиком* (решение владельца, «зачем выжимка если и так обрабатываем»): накопительные `<проект>.md`, LLM-выжимка (para-extract IPC, backend `cmd_extract`, событие `extracted` из events.json, preload-биндинг), архивация в «Исходные встречи». Раскладка = classify + move, без второго LLM-вызова.
- **PR #35** (`1643588`): CLAUDE.md § Артефакты — конвенции (схема при структурных сдвигах, PR-walkthrough батчей ≥3 PR, инцидент-страницы на класс ≥2 повторов). Зеркально: G-правило артефактов в `~/.claude/local-forks/_tracked/general-rules.md` + шаг 1b (fetch схемы) в скилл /meeting-recorder — запушено в local-forks (`dcae178`).
- **PR #37** (`b83496e`, v1.4.11, фикс-цикл после живого смоука владельца): (a) **pending-строка рейла** — заголовок схлопывался в вертикальную колонку по букве: рейл жёстко 280px + кнопка стала подписанной («▶ Обработать», T1) + унаследованный `.queue-name { word-break:break-all }` (style.css:640, под широкую очередь импорта) → scoped-правило `.rail-item.pending .queue-name` с ellipsis/`word-break:normal`/`min-width:0`; (b) бейджи рейла («ждёт обработки») ломались на 2 строки → `white-space:nowrap` на `.rail-rec-badge`; (c) шапка открытой заметки и строки спикеров («это я») вылезали за карточку на узкой ширине → `flex-wrap` на `.note-actions`/`.speaker-row`. Локи: 2 CSS source-text теста в renderer.test.js (идиома main.js-source проверок — первая CSS-вариация). Верификация: скриншот-прогон дев-инстанса (playwright-рецепт) на 760px + живой `getComputedStyle`.

**Решения владельца (сессия):** вариант A оба (явные кнопки; сегменты); схема папок «как в надиктовке»; снос накопителей — ок; выжимка — выпилить (вариант a); файлы НЕ переименовывать в «дата тема.md» (identity Истории по stamp в имени — жёсткий лок, отклонение от надиктовки принято); walk(root,4) — headroom ок; версия — патч-бамп 1.4.10 докинут в открытый PR.

**Проваленные подходы / инциденты:**
- **Org monthly spend limit убил developer-агента** на резюме (дельта «выпилить выжимку») — до первого edit'а, ветка цела. Дельту доделал оркестратор инлайн (обоснование: тир агентов недоступен). При лимите — проверять `git status` на частичные правки до продолжения.
- Критик поймал 6 миноров (протухшие комментарии, перевранные под-счётчики тестов в отчёте developer'а 13→9/8→10) — все закрыты в `dac55a1`.
- **v1.4.10 ушёл с overflow-регрессией при 979 зелёных тестах**: T1 сделал pending-кнопку подписанной, но никто не прогнал глазами узкий рейл с pending-строкой (в тест-профиле pending нет; jsdom лейаут не считает). Владелец поймал на первом живом запуске. Урок: UI-правки рейла/карточек — скриншот-прогон драйвером ДО PR (рецепт в скретчпаде: `_electron.launch` + `MEETING_RECORDER_USER_DATA`-изоляция), особенно состояния, которых нет в чистом профиле (pending, running, failed).

**Ловушки (новые):**
- `.queue-name` — ОБЩИЙ класс очереди импорта (Запись, широкая) и pending-строк рейла (280px): `word-break:break-all` в нём правильный для первой и фатальный для второй. Правки — только scoped-правилами.
- `MEETING_RECORDER_USER_DATA`-профиль (e2e/скриншот-прогоны) пуст: pending-записей нет → pending-строку рейла живьём в нём не проверить; presets.json при этом читается из корня репо (реальный vault виден).
- Мок `paraExtract` в renderer.test.js — **бросающий сентинел**: воскресший вызов выжимки в раскладке упадёт громко. Не заменять на рабочий мок.
- `_scan_audio_inventory` non-recursive: аудио, уехавшее раскладкой в глубокие папки vault, невидимо инвентарю (было и до — «Исходные встречи», класс прежний).
- Несколько заметок одной записи в РАЗНЫХ папках → аудио уезжает с первой (mv src gone → null). Владельцу озвучено, принято.
- Артефакт-схема обновляется re-publish'ем: исходник = сохранённый WebFetch-HTML, чистить от frame-runtime (`sed -n '/<title>/,/claude-mermaid-runtime-begin/p'`), publish с `url`-параметром.

**Следующие шаги:**
1. **Дожать живой смоук на v1.4.11** (владелец начал на 1.4.10 — нашёл overflow; остальное не пройдено): pending-строка (ellipsis вместо столбика — не проверена живьём, в тест-профиле pending нет), раскладка «Разложить» на реальной заметке (one-to-one → «один-на-один с <Имя>», архив → 2026/07, История не теряет заметку после переезда), корзина, узкая ширина шапки заметки.
2. `Pipeline.summarize()` без ru-пина (находка developer'а, не тронуто — вне скоупа батча).
3. Хвост критика: аудио-сплит при разложении заметок одной записи в разные папки — если всплывёт на живых данных.
4. Зеркало Claude Design: screens/para.html и history-карточки устарели после #36 (сегменты, явные кнопки) — концепт-карточки вариантов A/B этой сессии лежат в проекте (`screens/history-buttons-*`, `screens/para-horizontal-*`), полное зеркало всё ещё ждёт переписки build_screens.py (сломан с #27).
5. Прежние: mutmut 82→85%, гейт релиза на e2e/mutation (за владельцем), StrykerJS, minor-хвосты #30, эмодзи-стек, иконка .app, калибровка auto-«Я».

**Открытые вопросы:** гейтить релиз на e2e/mutation — за владельцем (висит с прошлой сессии); ревью-чат Claude Design; эмодзи/радиусы (с #27).

**Быстрый старт (свежая сессия):** `cd ~/MeetingRecorder/app` (снимает push-гейт-цирк) · `git fetch && git merge --ff-only origin/main` · тесты `npm test` · артефакт-схема (обновлять при структурных сдвигах, § Артефакты CLAUDE.md): https://claude.ai/code/artifact/c6589f95-071a-424b-b567-a34dcf792265 — обновлена под v1.4.10 (сегменты, иерархия, снос выжимки; CSS-фиксы #37 структуру не двигают) · скриншот-драйвер UI: см. Ловушки (playwright-рецепт) · первое действие: живой смоук шага 1.

## Сессия 2026-07-18/19 — архитектурная схема, аудит+фиксы, Кальницкий-батч, вкладка Корзина (v1.4.5–v1.4.9, PR #30–#34)

Статус: **всё смержено и выпущено**. Сессия из `~/SupersetWorkspace` (push-гейт — по разрешению владельца на каждый push, схема прежняя; маркер одноразовый — сгорает и на ОТКЛОНЁННОМ пуше, переставлять).

**Проверено в этом прогоне (команда → вывод):**
- `git log -1 --oneline` (main после ff) → `84581fe feat(trash): вкладка Корзина … (#34)`; в логе #30–#34 подряд поверх d084842.
- `npm test` → **JS 592/592 pass**; `pytest -q` → **PY 358 passed**.
- `gh release view v1.4.9` → тег есть; `gh pr list --state open` → 0 открытых.
- `defaults read /Applications/.../Info.plist` → установлен **1.4.9** (ставил 1.4.8 руками — автообновлялка дотянула до 1.4.9 сама, механизм не наблюдал напрямую).

**Что доехало (флоу: developer-агент → критик-гейт → push с разрешения → PR → владелец мержит):**
- **PR #30** (`594afbb`, v1.4.5): 8 фиксов архитектурного аудита (analyzer, 9 находок): H1 `busyVerdict()` централизован + PARA-хендлеры загардены; H2 `isPathInsideRoots()` на read-note/rename-speakers/reveal/para-*; H3 `isFileStable()`-гейт (закрыл инцидент 2026-07-10 «0.4с из 25-мин») + `contentFingerprint` в cache-key (старые кэши инвалидированы разово); M4 `events.json`+`lib/events.js` контракт имён событий (assert в emit(), Python cross-lock); M5 regex-тест зеркала model-путей; M6 procProc-гейт на remove-pending; L7 saveToken/rmSync surfacing; L9 снесены sourceBadge и backend pending-merge путь (`kind:"pending"`, `--pending-file`).
- **PR #31** (`8729498`, v1.4.6, ХОТФИКС): **v1.4.5 падал на старте packaged** — `lib/events.js` require'ил events.json из asar, а его там НЕТ. Root cause A/B-верифицирован парой сборок: **корневой файл в `build.files` И `build.extraResources` одновременно → electron-builder молча НЕ кладёт его в asar** (7-й asar-баг проекта). Фикс: CONTRACT_CANDIDATES fallback asar-путь → `process.resourcesPath`. Юзерам на 1.4.5 нужна была ручная переустановка (app не стартует → обновлялка мертва); владельцу поставил руками из release-zip.
- **PR #32** (`abf01fa`, v1.4.7): **e2e boot-smoke** `e2e/boot.test.js` — playwright-core `_electron` против `electron-builder --dir` артефакта (окно + `.sidebar-nav` + `window.api`); red→green proof на v1.4.5-баге; CI-джоба `e2e-boot` (macos-14, release-гейт НЕ тронут); `MEETING_RECORDER_USER_DATA` env-override в main.js (изоляция userData, no-op в обычных запусках); корневой `CLAUDE.md` с конвенциями. `E2E_LAUNCH_TIMEOUT_MS` — таймаут для холодных раннеров.
- **PR #33** (`dcb4100`, v1.4.8): **`backend_core.py`** — move-рефактор чистых функций (глоссарий-цепочка, `_base_stamp`, auto-«Я» скореры, combine/add_timestamps как staticmethod-делегаты; тела byte-identical, критик сверил по ханкам). НЕ перенесены (impure): filter_hallucinations, estimate_start_offset_ms (зовут log()), _build_initial_prompt (self-стейт). `tests/test_backend_core.py` отвязан от backend.py. **mutmut** (requirements-dev.txt, НЕ в юзерский инсталл): скор **~82%** (680/829), выжившие кластеры gate_llm_correction 33 / _diff_term_hits 17 / _normalized_xcorr_peak 14; CI-джоба `mutation` report-only (~34с). Packaging: backend_core.py в extraResources (только там!), проверен спавном с cwd=Resources.
- **PR #34** (`84581fe`, v1.4.9): **вкладка 🗑 Корзина** (дизайн — вариант A из Claude Design, выбран владельцем): строки по манифест-записям, «осталось N дн» (warn <7), ↩ Восстановить / Удалить навсегда / Очистить (confirm), empty state. Манифест для НОВЫХ удалений расширен `origin`/`title`/`id`; легаси — restore в outDir (best-effort), backfill id при list. IPC list/restore/delete/empty-trash. **Критик-раунд поймал критичное:** restore писал по origin-пути из редактируемого манифеста без containment (write-примитив) — закрыто `39f8549`: `restoreDestinationFor` честит origin только внутри roots, иначе fallback outDir; traversal (`../../etc`) нормализуется и режется, залочено тестом.

**Вне репо:**
- **Артефакт схемы архитектуры** (обновлять при структурных сдвигах): https://claude.ai/code/artifact/c6589f95-071a-424b-b567-a34dcf792265 — топология, пайплайн, хранилища, IPC-группы, 6 видов, чистые ядра, тест-слои. Исходник в скретчпаде сессии (выметается) — редактировать через re-publish с тем же URL.
- **Claude Design:** добавлены `screens/trash-tab-a.html`/`-b.html` + `_ds_manifest.json` дописан вручную (write_files; register_assets манифест НЕ пересобирает — ловушка прежняя). Выбран A. Ревью-чат «что ещё поправить» владелец не создавал.
- **Конспект Кальницкого** (`~/Downloads/Кальницкий_агенты_вайбкодинг_2026-07-01*.md`) — источник батча #32/#33: приёмочные на поднятом приложении, чистая модель без зависимостей, мутационное тестирование, TDD red→green в промптах агентов (теперь в CLAUDE.md репо). НЕ перенесено осознанно: 3-проектный onion (.NET-форма), его экономика.

**Решения владельца (сессия):**
- Нарезка батча фиксов: **«Один большой PR»** (против рекомендации 4 PR) — ушло в K1-правило (батч одного мотива → дефолт один PR).
- Blanket-обход push-гейта на батч (окно = до открытия PR); отдельные push'и — по отдельному разрешению каждый.
- sourceBadge + backend pending-merge — **снести** (решение принято, ранее висело).
- Корзина — вариант A (список), не B (карточки).
- L8 (извлечение updater/models из main.js) — отложен, не дефект.

**Проваленные подходы / инциденты — НЕ ПОВТОРЯТЬ:**
- **v1.4.5 релиз с крэшем при 903 зелёных тестах:** критик #30 написал «packaging mitigated by package.json entries» — конфиг проверен, РЕАЛЬНЫЙ артефакт нет. Урок: files+extraResources → omission; верифицировать `npx @electron/asar list` на построенном билде; системный лок — e2e boot-smoke (теперь в CI). В g-правиле `g-electron-asar-path-audit`.
- `gh pr checks --watch` сразу после `gh pr create` → «no checks reported», exit 1 (PR #33): чеки регистрируются с лагом — sleep ~30с/poll до watch (K2-правило).
- Первый push reflect-репо отклонён (remote ушёл вперёд с другой машины) → одноразовый bypass-маркер СГОРЕЛ на отклонённом пуше; rebase-конфликты только в генерируемых INDEX.md/remote.json — региенерировать скриптами, не мержить руками.

**Ловушки (новые, поверх прежних):**
- `backend_core.py` ДОЛЖЕН ехать в extraResources рядом с backend.py (sibling-импорт через sys.path[0]=script dir); в `build.files` НЕ добавлять (omission-класс).
- mutmut требует полной развязки test_backend_core.py от backend.py (один импорт константы ломал sandbox — инлайнить литерал).
- `.trash/manifest.json` руками не править: origin-пути вне roots молча уйдут в outDir-fallback при restore (защита, не баг).
- e2e требует собранного `dist/` (`npx electron-builder --mac --arm64 --dir`) — тест падает с внятным сообщением, не скипается.
- Маркер push-гейта одноразовый и сгорает на ЛЮБОЙ попытке push, включая отклонённую.

**Следующие шаги:**
1. **Живой смоук v1.4.9 руками** (так и не сделан с v1.4.4!): рейл Истории на реальных данных, ✕ → Корзина → restore → «файл вернулся, История видит», purge (подделать deletedAt), «Очистить корзину».
2. Дотяжка mutmut 82→85%: кластеры известны (gate_llm_correction 33, _diff_term_hits 17, _normalized_xcorr_peak 14).
3. Решить: гейтить ли релиз на e2e-boot (стабильность джобы уже 3 зелёных прогона) и/или mutation.
4. StrykerJS на mainutil (JS-мутационка, отложена из #33).
5. Minor-хвосты критика #30: para-classify читает по сырому root/folders (только --note валидирован); resolved-vs-non-resolved roots при симлинках; обратный контракт-лок M4 (emit-call-sites ∈ contract).
6. Прежние хвосты: зеркало Claude Design под сайдбар-DOM (build_screens.py сломан с #27), эмодзи-стек, иконка .app, калибровка auto-«Я» порогов (HYPO, одна запись).

**Открытые вопросы:** гейтить релиз на e2e/mutation — за владельцем; ревью-чат Claude Design (создавать?); эмодзи цветные vs монохром, радиусы 12 vs 14 — висят с #27.

## Быстрый старт (свежая сессия)
`cd ~/MeetingRecorder/app` (запускать сессию ИЗ репо — снимает push-гейт-цирк) · `git fetch && git merge --ff-only origin/main` · тесты: `npm test` (JS+PY), e2e: `npx electron-builder --mac --arm64 --dir && npm run test:e2e`, мутационка: см. setup.cfg · ключевые файлы: main.js, lib/mainutil.js, lib/events.js+events.json, backend.py+backend_core.py, renderer/renderer.js, CLAUDE.md · первое действие: живой смоук корзины (шаг 1).

# HANDOFF — состояние на 2026-07-18 (предыдущий)

Electron-приложение записи встреч: mic+system (AudioTee) / импорт → `backend.py` (JSON-stdout): mono/VAD → MLX Whisper → коррекция терминов → pyannote → LLM-сводка (LM Studio `:1234`) → `.md` в Obsidian. Архитектура и хранение — см. README.md. Пять видов (навигация в **левом сайдбаре**): 🎙 Запись · 📚 История · 🗂 PARA · 📖 Словарь · 📝 Промпты.

## Сессия 2026-07-16/18 — аудио-центричная История: удаление заметок, инвентарь аудио, новый рейл, корзина 30 дней (v1.4.4)

Статус: **всё смержено** (PR #28, #29), релиз v1.4.4 — джоба в полёте на момент хендоффа. Сессия из `~/SupersetWorkspace` (push-гейт обходился по разрешению владельца на каждый push, схема прежняя).

**Проверено в этом прогоне (команда → вывод):**
- `git log -1 --oneline origin/main` → `d084842 feat(history): аудио-центричная История — инвентарь + рейл (вариант A) (#29)`.
- `npm test` на main d084842 → **JS 501/501 pass · PY 355/355 passed**.
- `gh pr view 29` → `MERGED`; `gh release view v1.4.4` → «release not found», `gh run list --branch main` → верхний прогон CI ещё бежит (**не проверено завершение релиза — дождаться dmg/zip**).
- `package.json` → `"version": "1.4.4"` (в составе #29).

**Цель (слова владельца):** «в истории добавим возможность удалить заметки, при удалении всегда всплывает окно»; затем «списком идет аудио, а под ней список того как мы обработали»; «аудио не сносится окончательно, а помещается в корзину условную на 30 дней»; «всё в одном пре».

**Что доехало (флоу: developer-агент → критик-гейт → push с разрешения → PR, мержит владелец):**
- **PR #28** (`67dd366`): удаление одной заметки из открытой карточки Истории — confirm всегда, удаляется только `.md` (unlink; позже конвертирован в корзину в #29). IPC `delete-history-note` c валидацией: `lib/mainutil.js isNoteDeletable` — realpath + контейнмент в outDir/PARA-root + `.md` на сыром И resolved-пути (симлинк-кейс поймал критик).
- **PR #29** (`d084842`, v1.4.4) — ЕДИНЫЙ PR, три куска:
  - *Backend*: `_base_stamp(stem)` (backend.py ~2218, вынесен из `_find_audio`, parity залочен) → `base_stamp` на каждой строке заметки cmd_history; `_scan_audio_inventory` (~2261) → top-level `audios[]` `{base_stamp,path,size,mtime,duration_s}` — один scandir out_dir, сироты включены, `duration_s` из WAV-заголовка (stdlib wave, null при ошибке), дотфайлы/дотдиры скипаются, в подкаталоги не рекурсит.
  - *Рейл (дизайн «Вариант A»)*: `buildRecordings()` (renderer.js) — единый хронологический список записей: группы заметок по `base_stamp` + `audios[]` (сирота → «без обработок» + «▶ Обработать» через reprocess-пайплайн) + pending (влит в общий список, бейдж «ждёт обработки», своя ✕-семантика не тронута). Дата-заголовки считают записи. Разворачиваемые группы: обработки = шаблон + vN/latest + язык, клик открывает заметку, ✕ на строке → delete-note флоу. Старый рейл (`groupByBaseStamp`/`buildHistoryRow`/`buildHistoryVersionGroup`) и дубликат `baseStampOf` УДАЛЕНЫ; main.js list-history прокидывает `audios`/`base_stamp`.
  - *Корзина*: ✕ на строке записи (не pending) → confirm с перечислением (аудио + N заметок) → всё в `<root>/.trash/` (`trashRootFor`: vault или outDir-sibling; Obsidian-конвенция, реконсайл и инвентарь дотдиры скипают — проверено backend.py:2279/2309/2324). `moveToTrash` rename + EXDEV copy→unlink fallback, коллизии суффиксуются; `.trash/manifest.json` атомарно `{deletedAt,kind,files,baseStamp}`; `purgeTrash` на старте (>30 дней → перманентное удаление; пропавший файл → drop entry; фейлы не блокируют старт; containment-гейт — файлы только внутри trashDir). `delete-history-note` конвертирован unlink→корзина. `isAudioDeletable` — тот же симлинк-урок (extension-гейт на resolved).
- Версия: `1.4.3 → 1.4.4` (владелец поправил мой 1.5.0: «минорную» у него = патч-цифра; коммиты `56c44e7`+`2c666f1`).

**Критик-гейты (4 прохода):** #28 — approve + hardening (resolved-`.md`); backend — approve + тривии (лишние локальные импорты, тест не-рекурсии); рейл — approve (девиации разработчика признаны разумными); корзина — **fix-required**: partial-move оставлял протухший рейл (renderer возвращался до `refreshHistory()` при `ok:false`) — закрыт `b28d5cb`, фикс верифицирован ревертом.

**Дизайн через Claude Design** (проект «MeetingRecorder Design System» `e7e0e469-…`): 3 новые карточки в группе Screens — «История — аудио · Вариант A» (выбран владельцем, + ✕ на обработках по его правке), «Вариант B» (карточки+чипы, отвергнут), «корзина и статусы». Монолитная первая карточка была не видна целиком (варианты в ряд ~1150px шире вьюпорта панели) → разбита на три. `_ds_manifest.json` правился ВРУЧНУЮ через write_files (register_assets его не пересобирает — прежняя ловушка актуальна). Исходники карточек в скретчпаде сессии (выметается) — восстанавливать из проекта через get_file.

**Решения владельца (сессия):**
- Удаление заметки: только `.md`, аудио остаётся («позже можем переобработать»); по одной версии, не группой.
- История аудио-центрична: запись → обработки; вариант A (дерево), не B; ✕ и на строках обработок.
- Аудио не удаляется навсегда — корзина 30 дней. Pending — в общий список. Заголовок записи = title последней заметки.
- «Всё в одном пре» (сказано трижды, с эскалацией) — inventory+rail+корзина слиты в PR #29; отдельные PR отвергнуты.
- Версии: фича-PR = ПАТЧ-бамп (1.4.4). «Минорную апни» у владельца = последняя цифра — не пересаживать на semver-минор без вопроса.

**Проваленные подходы / инциденты — НЕ ПОВТОРЯТЬ:**
- Мой план «3 отдельных PR» дважды противоречил владельцу — при «в один PR» сразу спрашивать какой именно PR целевой (он имел в виду СУЩЕСТВУЮЩИЙ #29, я сначала слил только 2 куска из 3).
- Rail-агент застрял на 600с (stream watchdog, совпало с протухшей авторизацией `/login`) — ветка была создана, коммитов ноль; лечится SendMessage-резюмом с чекпоинтом состояния.
- `gh pr create` для 1a ушёл при main уже уехавшем на #28 → PR родился BEHIND → `gh pr update-branch 29` (это добавило remote merge-commit, локальная ветка потом синкалась `--ff-only origin/...`).

**Ловушки (новые, поверх прежних):**
- `_ds_manifest.json` Claude Design — только ручная правка через write_files, иначе карточка невидима.
- Широкие концепт-карточки (>~800px) режутся вьюпортом панели дизайн-системы — делать по карточке на вариант.
- `sourceBadge`/`SOURCE_BADGE_*` (renderer.js ~2063) — мёртвый код после нового рейла, оставлен сознательно (владельцу озвучено, решения нет). Внутри — известный анти-паттерн `title="${escapeHtml(...)}"` — при воскрешении НЕ копировать.
- Тест-бейслайны в промптах агентам протухают: JS на main был 463 (не 448 из HANDOFF) — агенты мерили сами; писать «measure on main first».
- pending-merge путь backend cmd_history (`kind:"pending"` строки) всё ещё жив и всё ещё игнорируется renderer'ом — ретиремент так и НЕ сделан (был в спеке 1b, агент не тронул — не эскалировано).

**Следующие шаги:**
1. **Дождаться релиз-джобу v1.4.4** (`gh run list --branch main` / `gh release view v1.4.4`) — на хендофф не завершена.
2. Живой смоук нового рейла + корзины (первый реальный запуск = smoke): группировка реальных заметок, сирота после удаления всех заметок, ✕ записи → `.trash`, purge (подделать deletedAt в manifest.json).
3. Хвосты: мёртвый `sourceBadge` (снести?), ретиремент backend pending-merge пути, duration у pending-строк (нет источника), дефолт-развёрнутость групп.
4. Зеркало Claude Design под новый рейл (`screens/history.html` устарел ещё с #27, теперь и структурно).
5. Прежние хвосты: эмодзи-стек, иконка .app, двойной «(latest)» (возможно закрыт новым рейлом — проверить на живых данных), калибровка auto-«Я».

**Открытые вопросы:** судьба `sourceBadge`; цветные эмодзи vs монохром; радиусы 12 vs 14; collapse-политика групп рейла.

## Сессия 2026-07-15/16 — дизайн-цикл через Claude Design: токены/AA → лейауты → редизайн (сайдбар+темы), v1.4.1–v1.4.3

Статус: **всё смержено и выпущено**. Сессия из `~/SupersetWorkspace` (push-гейт обходился по разрешению владельца, схема ниже без изменений).

**Проверено в этом прогоне (команда → вывод):**
- `git log -1 --oneline` (main после ff) → `366ab72 Redesign: sidebar navigation, 4 selectable themes… (#27)`; в логе #25 `a5925a6` (v1.4.1), #26 `03f7303` (v1.4.2).
- `npm test` → **JS 448/448 pass · PY 333/333 passed** (прогон этой сессии, main).
- `gh release view v1.4.3` → ассеты `Meeting.Recorder-1.4.3-arm64.dmg` + `-mac.zip`; CI на main ok.
- `package.json` → `"version": "1.4.3"`.

**Что доехало (3 PR, флоу: developer-агент → критик-гейт → пуш → владелец мержит):**
- **PR #25** (`a5925a6`, v1.4.1): дизайн-токены+AA. `--accent-fill #3a6fd0`/`--accent-fill-hover #3160b8`/`--danger-fill #d3363b` (заливки были 3.3–3.9:1 к #fff); `--warn/--pre-bg/--overlay/--ok-tint/--danger-tint` вместо литералов; `:focus-visible` на все кнопочные (WCAG 2.4.7 — раньше не было вообще); `--rec #f06a3c`/`--rec-fill #c94318` (запись ≠ ошибка), `--selection #2b4a86` (выделение ≠ hover), `--accent2` удалён; шкалы: шрифты 14→8 ступеней, радиусы 4/8/12+pill(999), паддинги на 4px-грид; `--inactive #6b7480`+dashed вместо opacity у skip/canceled (canceled: зачёркнут + CSS `::after` «· отменено»); хит-зоны chip-кнопок 24px.
- **PR #26** (`03f7303`, v1.4.2): лейауты. Двухпанельные вью (История/PARA/Промпты) 860→1200px через `main:has(...)` (Chromium 130 проверен живым вызовом); рейл 240→280; PARA-строки в 2 яруса (`.para-row-top`/`.para-row-controls`; `.para-proj` — это INPUT, не select); фильтры Истории за тоглом с бейджем активных; пустые состояния (История: CTA «Начать запись»; PARA: «Всё разобрано») + автооткрытие последней заметки; Запись в 2 колонки ≥1100px + sticky action-bar (кнопки процессинга co-located, НЕ объединены — объединение меняло бы видимость stop/spinner в гоночной зоне, осознанно отложено). Критик поймал блокер до пуша: `reprocessHistory()` не скрывал перенесённый `#processLatestBtn` → мог misfire на последнюю запись; закрыт + regression-тест (проверен ревертом фикса).
- **PR #27** (`366ab72`, v1.4.3): редизайн. (C) сайдбар 196px — лого, 5 nav-пунктов (класс `.topbtn` сохранён), блок «Идёт запись» с живым таймером внизу (виден из любой вкладки; таймер бродкастится на оба readout через `querySelectorAll(".timer")`), `#settingsOpen` внизу; body→flex row, **скроллится `main`, не body**; sticky-офсеты рейлов пересажены на top:16px. (B) 4 темы: `:root`=классика (байт-в-байт), `[data-theme="pervanche"|"teal"|"orchid"]` — только нейтрали/акцент/выделение, семантика rec/ok/warn/danger глобальна; новые токены `--accent-fill-text`/`--selection-text`/`--selection-muted` (у новых тем светлые заливки с ТЁМНЫМ текстом); селект «Оформление» в настройках, presets-поле `theme`, absent→classic; шрифты Unbounded (заголовки, размеры ужаты 18→15 и т.д.) + Onest (текст) — variable woff2 локально в `renderer/fonts/` + OFL-лицензии. (A) полировка токенами per-theme: `--grad-card/--shadow-card/--shadow-inset/--shadow-btn/--grad-primary/--shadow-primary/--rec-glow/--act-glow`.
- **Визуальный smoke сделан** (playwright-core `_electron`, dev-инстанс, 15 скриншотов, все 4 темы × Запись/История + модалка): темы переключаются целиком, персист работает, модалка поверх новой скролл-модели, «Текущая версия: 1.4.3 — актуальная». Драйвер-рецепт: `scratchpad-сессии/e2e/drive.js` (playwright-core ставится отдельно, `_electron.launch({executablePath: node_modules/.bin/electron, args:[APP], cwd:APP})` — работает без TCC-скриншотных прав).

**Claude Design (claude.ai/design) — рабочий процесс сессии:**
- Проект «MeetingRecorder Design System» `e7e0e469-046b-4799-9d40-d9c3a16da4b7` (владелец Artemii, выставлен private). 14 карточек: tokens/2 + components/7 + screens/5 — зеркало кода **на момент v1.4.2, после #27 УСТАРЕЛО** (см. следующие шаги).
- Флоу: DesignSync-тул читает/пишет файлы проекта (чата у тула НЕТ — промпты в чат вставляет владелец руками, ответы-canvas читаем через `get_file`); концепты выбирает владелец, переносит developer-агент точным спеком с hex-таблицами.
- Ревью-чат создаёт СВОЙ обычный проект («Design system review» `6379b099-…`) — в `list_projects` не виден (фильтр по design-system-типу), файл читается по id напрямую.

**Ловушки (новые, помимо прежних):**
- **`_ds_manifest.json` НЕ пересобирается `register_assets`** — панель Design System строится из манифеста; новые карточки/группы дописывать В САМ манифест через `write_files` (иначе группа невидима — так «потерялась» группа Screens).
- **Чат Claude Design снапшотит дизайн-систему в `_ds/…`-копию своего проекта при подключении** — после обновления карточек нужен НОВЫЙ чат, старый держит старую копию.
- **Скретчпад сессии выметается** — исходники карточек (bodies/ + build-скрипты) одноразовые, восстанавливаются из контекста/перегенерацией. `build_screens.py` инжектит данные точечными `.replace` по якорям из `index.html` — **после #27 якоря сломаны** (topnav→сайдбар), скрипт требует переписки под новый DOM.
- **Эмодзи рендерятся монохромно** после смены font-stack на Onest (fallback без `Apple Color Emoji`) — видно на скриншотах, владельцу показано, решение не принято.
- **Наблюдение в рейле Истории:** у группы версий две строки «Митинг — полная заметка · v2 (latest)» подряд — данные dev-`index.db`/поведение версионирования v1.3.0, НЕ регрессия #27; не диагностировано.
- rtk-хук мангуляет и `grep -c` выводы («N matches in 0 files») — для точных чисел `rtk proxy grep`.

**Решения владельца (сессия):** репо считать публичным-как-есть, дизайн улучшать циклами через Claude Design; вариант A контрастов (fill=бывший accent2); rec-цвет 2a (#f06a3c), selection 2d (#2b4a86); зелёный для записи отвергнут (коллизия с --ok); шкалы по маппингу 3a-3c; лейауты 4a-4c (4a сперва отложен, потом взят); редизайн C→B→A **в один PR**; вместо одной новой палитры — **все 3 темами** + классика, селект в настройках; версии — патчи (1.4.1/1.4.2/1.4.3); «в этом же PR» = докидывать коммиты в открытый PR.
- Радиусы: концепт B предлагал 14px, оставлены 12 (наша шкала) — **владельцу не эскалировано отдельно**, всплывёт при желании.

## Следующие шаги
1. **Перегенерить зеркало в Claude Design под v1.4.3**: переписать `build_screens.py` под сайдбарный DOM (якоря `topbtn active`/`view-record` сменились структурно), тела компонент-карточек — актуальны, но CSS в них вшит старый → пересборка всех 14 + `_ds_manifest.json` уже содержит все пути (write_files тем же списком).
2. Эмодзи-стек: если владелец хочет цветные — добавить `"Apple Color Emoji"` в `--font-body`/`--font-heading` fallback (style.css `@font-face`-блок и токены шрифтов), патч-PR.
3. Иконка приложения (`assets/icon.icns` + `mac.icon`) — дизайн-дыра из TODO, Claude Design может дать концепты.
4. Разобраться с двойным «(latest)» в группе версий рейла (см. Ловушки).

## Открытые вопросы
- Цветные эмодзи vs монохром (стиль?) — за владельцем.
- Радиусы 12 vs 14 из концепта B — за владельцем.

## Сессия 2026-07-15 (2) — CI/авто-релиз пайплайн, PARA+История фичи, auto-«Я» mic-share, v1.4.0
`main` = `5540150` · статус: **всё смержено, готово**. Сессия шла из `~/SupersetWorkspace` (НЕ каталог репо — см. ловушку push-гейта).

**Проверено в этой сессии (команда → вывод):**
- `git log -1 --oneline` → `5540150 feat(para,history)… (#24)`; в логе #21-#24 подряд поверх `b73b120`(v1.3.1).
- `gh release view v1.4.0` → ассеты `Meeting.Recorder-1.4.0-arm64.dmg` + `…-mac.zip`, Latest.
- `gh api …/branches/main/protection` → required checks `["js-tests","py-tests"]` (мерж блокируется при красных).
- CI-прогоны (не гонял локально в этой сессии — из GitHub Actions): **JS 438 · PY 333**, зелёные на всех PR.
- `git worktree list` → один (`~/MeetingRecorder/app`); временные worktree'ы снесены.

**Что доехало в main (4 PR, каждый: developer/правка → критик-гейт → PR → владелец мержит):**
- **PR #21** (`41db455`): GitHub Actions CI. `.github/workflows/ci.yml` — `js-tests`(ubuntu, `npm ci`+`npm run test:js`), `py-tests`(ubuntu, `pip install -r requirements-ci.txt`+`python -m pytest tests/ -q`), `check-version`(читает package.json version, есть ли релиз-тег), `release`(macos-arm, `needs:[js-tests,py-tests,check-version]`, гейт `push+main+should_release`, `electron-builder --mac --arm64 --publish never`+`gh release create`). Новый `requirements-ci.txt` = минимум под мокнутые тесты: `pytest numpy scipy tqdm` (эмпирически выведен; **ffmpeg** ставится в py-tests job через apt — системный бинарь, cmd_mix/cmd_preflight гейтят `shutil.which("ffmpeg")`, venv-проба его не ловит).
- **PR #22** (`e306a9b`): пикер переобработки + прогресс-панель в Истории теперь НАД транскриптом (`insertAboveNoteBody` вместо `appendChild` в конец #noteView; `openReprocessPicker`/`buildHistoryProgressPanel`).
- **PR #23** (`842fe7f`): **auto-«Я» починен**. Метрика переписана с mic/system-ratio на **долю энергии mic-дорожки** (`compute_speaker_dominance`/`pick_author_label` backend.py). Убран жёсткий `len(raw_labels)<2` skip → single-спикер теперь тоже получает автора. Константы: `_AUTHOR_MIN_MIC_SHARE=0.40`, `_AUTHOR_MIN_MARGIN=0.15`, `_AUTHOR_MIN_DURATION_S=3.0`, `_AUTHOR_MIN_MIC_RMS=50.0` (int16-scale). RCA: system-трек несёт эхо голоса автора → старый ratio упирался в ~0.46 < порога 0.65. Верифицировано на реальном 7ic8: `SPEAKER_04 mic_share=0.53` → «Спикер 4 определён как автор».
- **PR #24** (`5540150`, **v1.4.0**): (a) PARA «Разбор» — пустая категория при «Разложить» (`fileParaRow`) → авто-классификация LLM (`paraClassify`, как батч), потом раскладка; (b) смена спикеров в карточке заметки Истории (`openHistoryNote` + `#nvSpeakerMap`, рефактор `buildSpeakerMap`→`renderSpeakerRows`/`readSpeakerMap`, через существующий `renameSpeakers` IPC — бэкенд не менялся); (c) бамп `1.3.1→1.4.0`.

**🎉 Авто-релиз v1.4.0 отработал вживую** (первый реальный прогон release-джобы): мерж #24 → тесты → `electron-builder` на macos-arm → `gh release create v1.4.0` (dmg+zip). Юзеры получат через автообновлялку.

**Ловушки (укусят следующего):**
- **push-гейт из чужого cwd:** сессия из `~/SupersetWorkspace` (не репо) → PreToolUse `auto-critic.sh` делает `git rev-parse origin/main` в cwd сессии → fail-closed ДО маркера, блокирует ЛЮБОЙ `git push`. Обход С РАЗРЕШЕНИЯ ВЛАДЕЛЬЦА каждый push: отдельной командой `git -C ~/SupersetWorkspace update-ref refs/remotes/origin/main HEAD` + `printf 'bypass: owner-accepted-risk\n' > ~/.claude-critic-approved`, потом `git push` (другой командой — хук блокирует весь compound с `git push`), затем `update-ref -d` + `rm marker`. **Правильный фикс — запускать сессию из `~/MeetingRecorder/app`.**
- **gh-токену нужен scope `workflow`** чтобы пушить `.github/workflows/*` — добавлен в этой сессии (`gh auth refresh -s workflow`, интерактивно). Уже есть теперь.
- **`.secret` — keychain-encrypted (`enc:` префикс), НЕ base64.** Для standalone-прогона `backend.py` с диаризацией (нужен HF-токен): `HF_HUB_OFFLINE=1` + дамми-токен если модели pyannote уже в кеше (не трогать реальный diarize()-путь).
- **Устаревший локальный main после мержей:** после мержа PR на GitHub локальный main отстаёт; ветка от него → PR «behind base», strict-protection блокирует. `git fetch && git checkout main && git merge --ff-only origin/main` ДО ветвления; для уже-behind PR — `gh pr update-branch <N>`.
- **Параллельные мутирующие агенты — ТОЛЬКО `isolation:'worktree'`.** В этой сессии backend-developer в общем дереве + мои renderer-правки → общий git-индекс → мой коммит смёл его staged backend-файлы на чужую ветку (поймано на `git diff --stat`).

**Открытые вопросы / хвосты:**
- auto-«Я» пороги — **HYPO, откалиброваны по ОДНОЙ записи (7ic8)**. Калибровать на большем числе реальных встреч. Особенно `_AUTHOR_MIN_MIC_RMS=50.0` (int16 ≈ −56 dBFS — низко, ambient-шум может пройти на соло-записи где ты молчал → ложный автор). Per-label `mic_share`/`mic_level`/`mic_ratio` логируются каждый прогон для тюнинга.
- `HANDOFF.md` НЕ закоммичен (этот апдейт + предыдущий 2026-07-08→2026-07-15 висят как working-tree M) — конвенция проекта, видимо, держать локально; при желании закоммитить — веткой+PR (main защищён).

## Сессия 2026-07-11/15 — надиктовка-бэклог, версионность, гонка записи, релизы v1.3.0/v1.3.1

Каждый чанк: developer → критик-гейт → PR → владелец мержит. `main` = `b73b120` (v1.3.1). Тесты **JS 434/434 · PY 323** (прогон 2026-07-15, `npm test`, зелёные).

**Что доехало в main / релизы:**
- **PR #16** (`d19111e`, v1.3.0): из надиктовки `meeting-2026-07-11-085549`. (a) «Переобработать» в Истории всегда резолвит аудио — `resolveReprocessAudio()` из embed заметки `![[..wav]]` + dirname, иначе disabled+тултип; (b) прогресс/логи переобработки in-place в Истории (`reprocessTargetsHistory`+`progressTargetIds()`, убран `switchView("record")`); (c) кнопки «🎙 К записи» (`#nvGoRecord`/`#ptvGoRecord`) в шапке заметки История+PARA; (d) сворачиваемые папки категорий словаря; (e) **версионность заметок по шаблону при переобработке** — файл `meeting-<stamp>[-lang]-r<seq>.md` (seq с диска, уникален), `version` во фронтматтере (per-template), сворачиваемая группа на запись в Истории (latest помечен); trigger new-file = `version is not None` (новый шаблон=v1 тоже уникальный файл, чтобы не затирать); retry упавшего из вкладки Запись оставлен overwrite (осознанно).
- **PR #18** (`c65af23`): фикс гонки записи (см. RCA ниже).
- **PR #19** (`9a0f8da`): кнопка «▶ Обработать» на вкладке Запись — обрабатывает текущую (последнюю) запись; блок пока идёт запись И пока сводятся дорожки (`awaitingRecorded`, сброс на `recorded`+`error`). `latestPendingRecording()` = последний элемент `state.pendingRecordings`.
- **PR #17/#20**: бампы; релизы **v1.3.0** и **v1.3.1** (`gh release`, dmg+zip, `--target main`). `/releases/latest` = v1.3.1. Автообновлялка v1.3.x подхватывает по semver.

**RCA бага «последнее аудио / транскрибация провалилась» (закрыт PR #18 + чистка данных):**
- Симптом: обработка жевала обрубок ~1.4с вместо полной записи.
- Корень: `stop-recording`'s async `"mixed"`-хендлер — closure над module-level мутабельным `session` (main.js). Читал `session` живьём; быстрый пере-старт (двойной старт, 2 записи 5с апарт) переприсваивал `session` до завершения async-микса → манифест-entry получал id/dir/mic/system новой записи, но `mixed`-путь (из ffmpeg-arg) старой → кросс-линк на 96КБ/3с стаб. `manifest.push` без дедупа → дубль id. Гвард старта (`recordProc||tee`) очищался до конца микса → двойной старт проходил.
- Фикс PR #18: снапшот `const sess = session` до async-микса; `mixInFlight`-флаг блокирует старт пока микс в полёте (сброс в onClose на всех терминалах); `upsertById` (lib/mainutil.js) вместо push в main+renderer.
- Усугубляло: **живой app держал стаб в памяти** (см. ловушку про трей) + resume-кеш стаба переиспользовался. Ручная чистка: правка `pending.json` (убрать кросс-линк+дубль), `kill` живого процесса, снос стаб-кешей+onm1.
- **Подтверждение что закрыто:** 2 полных транскрипта 2026-07-14 после чистки — 4d00 (35мин→VAD 1578с, 398 сегм, 22103 симв) и новая запись 7ic8 (10мин→256с, 70 сегм). Пайплайн (convert/VAD/transcribe) НЕ виноват — прямой `backend.py process` на реальном файле всегда даёт полный транскрипт; виноват был вход-стаб.

**Ловушки (укусят следующего — file-level):**
- **Закрытие окна ≠ выход.** MeetingRecorder на close окна прячется в трей, процесс жив, `state.pendingRecordings` в памяти НЕ перечитывает `pending.json` с диска. Правка pending.json на диске не подхватится живым app. Симптом: reprocess упорно жуёт старую/стаб-запись. Лечение: настоящий выход (трей→Выйти / Cmd+Q / `kill <pid>`), потом релонч. `pgrep -fl "Meeting Recorder.app/Contents/MacOS"` — проверить жив ли.
- **~/Documents под TCC** для процесса Claude Code (родитель — Superset.app, без Full Disk Access): Read-тул И `cat` дают EPERM на `~/Documents/Obsidian`. AppleScript в ДРУГОМ TCC-домене работает: `osascript -e 'do shell script "cp/ls/sed ..."'` и `read POSIX file` читают+пишут vault. Obsidian-операции — через osascript.
- **1.4с mono (44878б, `vad_map {"chunks":null}`) = сигнал стаба:** пайплайн получил ~3с обрубок, не реальное аудио. Пайплайн сам исправен.
- **`cacheDirFor` = sha1(`path:size:mtimeMs`)[:16]** (lib/mainutil `cacheKey`, main.js:869). Resume переиспользует закешированный `mono.wav` («из кеша») — застрявший стаб-mono резюмится вечно, пока не `fresh=true`/чистка кеша. Кеш в `$TMPDIR/meeting-recorder/cache/<hash>/`.
- **push-гейт из чужого cwd:** сессия из `SupersetWorkspace` (не репо) → PreToolUse `auto-critic.sh` (plugin-hook, НЕ в settings.json) делает `git rev-parse origin/main` в cwd сессии → fail-closed ДО маркера. Обход С РАЗРЕШЕНИЯ ВЛАДЕЛЬЦА (каждый push — свежее согласие): `git -C <session-cwd> update-ref refs/remotes/origin/main HEAD` → отдельной командой `printf 'bypass: owner-accepted-risk\n' > ~/.claude-critic-approved` → push → `update-ref -d`. Правильный фикс — запускать сессию из каталога репо.
- **gh pr --body с heredoc:** `$(cat <<'EOF')` c инлайн-бэктиками исполняет их как команды (мангуется) — использовать `--body-file`.

**Открытые хвосты:**
- Мелкие критик-ниты v1.3.0 версионности (не блокеры): date-header в Истории считает заметки, не видимые строки (свёрнутая группа скрывает N); `version` во фронтматтере пишется строкой `"3"` (round-trip через `int()`, DB-колонка INTEGER — ок). В TODO при желании.
- Транскрипт 4d00 положен вручную в `~/Documents/Obsidian/Meetings/meeting-2026-07-13-140158.md` (+ аудио рядом) — без LLM-сводки (не было summary-промпта владельца).
- `pending.json` бэкап: `~/Library/Application Support/meeting-recorder/recordings/pending.json.bak-*`.

**Решения владельца (сессия):** релиз как 1.3.1; версионность-дисплей = сворачиваемая группа на запись; Task-1 = резолвить аудио всегда; сводку на угаданном промпте НЕ генерить.


## ⚠️ Репо ПУБЛИЧНЫЙ + новый git-процесс (2026-07-08)

- `ArtemiiF/MeetingRecorder` — **публичный** (решение владельца, ради автообновлялки без токена), MIT LICENSE. Старый приватный переименован в `MeetingRecorder-private` (бэкап со старой историей).
- История переписана filter-repo: автор всех коммитов `Artemii <a.filanovskii@yandex.ru>` (прецедент reflectory), Co-Authored-By вырезан. **Новые коммиты: тот же автор (git config локально стоит), НИКОГДА не добавлять Co-Authored-By-трейлер** — решение владельца.
- **Прямой push в main агенту ЗАПРЕЩЁН** (owner-solo-private delegation отменилась на public). Флоу: ветка → критик-гейт → push ветки → PR → владелец мержит сам. Прецедент: PR #1.
- Секурити-скан истории перед публикацией: чисто (presets/index.db/.secret/записи никогда не трекались, секрет-паттернов нет).

## Сессия 2026-07-07/08 — LLM-фиксы, История, fast-model, автообновлялка

- **Коррекция терминов** (`68744b6`): юзер поймал «LLM недоступен» — реально `Read timed out (120s)`. Замеры на живом LM Studio: gemma-4-26b (reasoning) на 1712-символьном чанке жжёт ВСЁ на thinking (4000 cap → 157с, reasoning 3997/3999; 16000 cap → 457с, reasoning 15997, content=0 — спираль НЕ заканчивается, бюджет не лечит). Фикс: `_llm_correct_budget(chunk_len)` — max_tokens = 4000 + ceil(chars/3) (floor = измеренный минимум), timeout = 30 + max_tokens/15 (rate 15 tok/s консервативно от измеренных 25); except разведён Timeout/ConnectionError/generic; correct-кеш пишет `llm_ok` — degraded-результат больше НЕ залипает (missing key у старых кешей → recompute).
- **Быстрая модель для механики** (`83e7644`): настройка «Модель для быстрых задач» (presets `fastModel`, пусто = загруженная модель) → поле `model` в запросах ТОЛЬКО correct/suggest/title; summarize/chat/PARA нетронуты (thinking остаётся для сводки — решение владельца). Замер `google/gemma-3-4b` (скачана владельцем): 6.2с тёплый, finish=stop, реальный контент — ~70× быстрее 26b. Qwen не трогать (решение владельца).
- **История + pending** (`41c5f20`+`b94bff2`): pending-записи мерджатся в Историю НА БЭКЕНДЕ (cmd_history `--pending-file`, синтетические строки kind:"pending", хронологический мердж двух форматов stamp через `_parse_any_stamp`); ▶/✕ и «Обработать все» в ленте, поверх фильтров (решение владельца); блок из карточки Запись удалён, мёртвая большая «▶ Обработать» скрыта вне import-режима. Типизация заметок: frontmatter `source` recording/batch/file (новый `--origin`), legacy без поля → бейдж ❓ unknown (решение владельца); DB-колонка source через guarded ALTER TABLE.
- **Автообновлялка** (PR #1, `a499af7`): секция «Обновления» в настройках — check releases/latest (без токена), semver-сравнение (mainutil), скачивание с прогрессом/отменой, `ditto -xk` → `xattr -dr quarantine` → swap через `.old` с откатом → relaunch. Гонка данных закрыта 2 слоями (критик-fix): обратные guard'ы (запись/обработка/модели/бэкенд отказывают при updateProc) + пере-проверка занятости перед swap. EXDEV (другой том) → откат + честное сообщение. Только packaged; чистка `.old`/updates на старте. Bootstrap: v1.0.0 обновлялку не содержит — одна ручная переустановка.
- **Релиз v1.0.0 перенесён** в публичный репо (ассеты скачаны из private, пересозданы). **v1.1.0** — релиз с автообновлялкой (последняя ручная установка).

## Сессия 2026-07-08 (продолжение) — весь UI-бэклог надиктовки (PR #3-#6)

Надиктовка `meeting-2026-07-07-221042` закрыта целиком, каждый чанк: developer → критик-гейт → PR → мерж владельцем:
- **PR #3** (`a082b87`): «Спросить» убрана из Истории (был nav-шорткат в PARA-чат, сам чат жив); заголовок заметки на языке записи (пин в промпте generate_title, auto без изменений); честная ffmpeg-подсказка (ffmpeg давно в установке бэкенда).
- **PR #4** (`3323cf0`): 5-я вкладка «📝 Промпты» — редактор шаблонов переехал из карточки Запись (там остался компактный селект); «Обработать» из Истории — пикер с предвыбором шаблона заметки (раньше молча брался preset[0] после запуска); пресеты получили стабильные id (uuid, backfill в loadPresetsData).
- **PR #5** (`42e9f1d`): Словарь — живой фильтр «N из M», разделы «Мои»/«Стандартные» (106 дефолтных свёрнуты), «Импорт/экспорт текстом» (запятая ИЛИ перенос строки); backend отдаёт счётчики срабатываний (`glossary_usage` в done, `_diff_term_hits`), бейджи «N×», и **сортировка терминов по пользе перед ~224-токенной обрезкой Whisper-промпта** (`--glossary-usage-file`; без данных — байт-в-байт прежнее, regression lock). Ресёрч-обоснование: Otter/Dragon/Descript паттерны. V2 (категории) отложен — только если после V1 тесно.
- **PR #6** (`a1e7569`): системный звук в preflight серый (не красный) до подтверждения, «проверяется при записи» + tooltip, зелёный только при granted, вердикт от него не зависит; Бэкенд — «Проверить» показывает версии Python/ffmpeg, путь env, отметку времени, переустановка подписана «целиком» (монолитна); Модели — размер на диске (symlink-safe) + per-model «↻ Скачать заново» (чистка → scoped докачка).

## Сессия 2026-07-10 — надиктовки 08.07 закрыты (PR #9-#13), v1.2.1; хотфикс v1.2.2

Из двух надиктовок (meeting-2026-07-08-184655 «История-концепт» + 185250 «UI-бэклог»), каждый чанк: developer → критик → PR → мерж владельцем:
- **PR #9** (`6440484`): root cause «в истории опять пусто» — PARA-разбор ФИЗИЧЕСКИ переносит .md из Meetings, `_reconcile` дропал строку. Теперь cmd_history сканирует outDir + весь vault (`--vault-root` из para.root): разобранная заметка остаётся в логе с обновлённым путём (identity = stamp из имени файла); удалённая с диска — уходит; `.obsidian`/`.trash` пропущены; symlink-safe (followlinks=False). Дата-разделители в ленте «8 июля · N» (счётчики по отфильтрованному); pending сверху. История = лог обработок (концепт владельца), НЕ временное хранилище до PARA.
- **PR #10** (`c7a82be`): «Обновления» — первая секция настроек; автопроверка версии при каждом открытии настроек (check-only, guard от конкурентных); фикс устаревшего HF-хинта (токен нужен для самой диаризации).
- **PR #11** (`5f35bc0`): кнопка «📋 Путь» в открытой заметке (История + PARA-Хранилище) — абсолютный путь в буфер (для вставки в Claude).
- **PR #12** (`f47ae8e`): `mainModel` — модель для основных задач (summarize/infer_speakers/extract_actions/classify/search-answer; пусто = загруженная, thinking живёт); механика остаётся на fastModel; оба поля — datalist из LM Studio `/api/v0/models` (embeddings отфильтрованы по type, 3с таймаут → тихий []), ручной ввод сохранён.
- **PR #13** (`fa7c85e`): словарь V2 — «Мои» группируются по категориям Люди/Продукты и инструменты/Термины/Другое (таксономия provisional); «Разложить по категориям» — LLM fastModel + жёсткий код-гейт (инвенции дроп, левая категория → Другое, reasoning_content-fallback); per-chip select; хранение аддитивно (`glossaryCategories` {termLower: category} рядом со строкой, mirror для CLI не тронут).

Свежий инцидент 2026-07-10 (диагностирован, НЕ чинен): 25-мин запись 12:29 → в пайплайн попало 0.4с (кеш mono.wav 12КБ при живом 48МБ mixed) — гонка «стоп записи ↔ старт обработки» (всё в одну секунду 12:54): файл, видимо, ещё дописывался. Битый кеш снесён, исходник цел в recordings/, восстановление через ▶ pending-строки. TODO: guard на pending-▶ до полной финализации mixed (см. TODO.md).

## Хотфикс v1.2.2 (2026-07-10) — updater ENOTDIR на app.asar

Живой фейл первого прогона автообновлялки (1.2.0→1.2.1): диалог `ENOTDIR ... rmdir .../updates/extract/.../app.asar`. Root cause: Electron fs-патч выдаёт app.asar за каталог → рекурсивный `fs.rmSync` по распакованному .app зовёт rmdir по файлу. Бил stale-extract wipe на ретрае (в try → диалог); тот же rm в finally и cleanupUpdateLeftovers глотался bare catch → мусор копился. Фикс: `rmNoAsar` (process.noAsar на время rm, restore в finally) на всех updater-rm, могущих встретить .app; zip-rm остались plain (единичный файл); шаги свапа именованы в ошибках («очистка старой копии»/«перенос текущей версии»/«установка новой версии»/«откат»). Класс — пятый asar-баг проекта (g-electron-asar-path-audit), первый в ЧИСТКАХ. Unresolved-загадка инцидента: .app.old оказался 1.4МБ ЧАСТИЧНОЙ копией (rename такого не делает) — не реконструировано, именованные шаги свапа дадут атрибуцию при повторе. Известный pre-existing риск (критик): фейл самого отката оставляет бандл сломанным — за владельцем.

## Упаковка в .app + установка бэкенда по кнопке (2026-07-07)

Решение владельца: **тонкая .app**, тяжёлый Python-бэкенд НЕ бандлится, а ставится по кнопке в `userData` на машине юзера. Цель — самодостаточный arm64 .app на другой Mac; подпись ad-hoc (без Apple Developer).

- **requirements.txt** (`b5682da`): pip freeze venv, 107 пакетов, py3.11.15. `openai-whisper` свапнут git→PyPI `==20250625` (убрал требование git при установке). pytest оставлен (dev-only, тримить потом).
- **Кнопка «Установить бэкенд»** (`b5682da`, секция «Бэкенд» в настройках, sibling «Модели»): качает python-build-standalone (arm64 CPython 3.11.15, sha256-пин) + статичный arm64 ffmpeg (sha256-пин) → `pip install --find-links vendor/wheels -r requirements.txt` со стримом прогресса. **Атомарно**: весь стек ставится в `BACKEND_ENV_STAGING`, `fs.rename` на `BACKEND_ENV` только после pip+маркера — partial/cancel никогда не виден как installed (это был blocker критика). `backendAvailable()`/`pythonBin()`/preflight/status все **marker-gated** (единый источник правды, не existence). IPC install/cancel/status/uninstall, busy-guard vs запись/обработка/скачивание. Полная чистая установка проверена живьём (симуляция чистого Mac, brew срезан, exit 0, всё импортится).
- **PyAudio** (`b5682da`, `vendor/wheels/pyaudio-0.2.14-cp311-cp311-macosx_11_0_arm64.whl`): у PyAudio нет macOS-wheel на PyPI (только Windows+sdist, компилится против системного portaudio). Решение: собран delocate-wheel с вшитым `libportaudio.2.dylib` (`@loader_path/.dylibs/`, ноль homebrew-ссылок), ретаргечен на big_sur bottle → тег `macosx_11_0` (работает на macOS 11+). Ставится из локального wheel — ни компилятора, ни brew на машине юзера.
- **electron-builder** (`ee85c03`, `26.15.3` пин): mac dmg+zip arm64, ad-hoc `identity:"-"` + `hardenedRuntime`. `build/entitlements.mac.plist` (`device.audio-input` + `disable-library-validation`) на `mac.entitlements` И `entitlementsInherit`; Info.plist `NSMicrophoneUsageDescription`+`NSAudioCaptureUsageDescription` через extendInfo. `extraResources`: backend.py+requirements.txt+vendor/wheels; `asarUnpack` audiotee bin. **Баг #9529** (ad-hoc ломает mic на v26.0.13+) решён через правильные entitlements (hardened runtime требует audio-input на main+helpers), НЕ через afterSign-хак/пин старой версии. Два packaged-бага пойманы прогоном бинаря: `WRITABLE_DIR` (presets/db/recordings/.secret уезжали в read-only asar — теперь userData при packaged, APP_DIR в dev) + spawn `cwd` (asar-virtual APP_DIR → `ENOTDIR`, чинено на `dirname(BACKEND)`). Билд: `npx electron-builder --mac --arm64`; `dist/` в .gitignore.

### Кнопки разрешений в Preflight (`545a0d7`)
Preflight-панель: у строки Микрофон — кнопка «Разрешить» (`systemPreferences.askForMediaAccess("microphone")` — нативный промпт при not-determined; после → refresh) / «Открыть настройки» при denied|restricted (диплинк `Privacy_Microphone`). У Системного звука — «Открыть настройки» (диплинк `Privacy_ScreenCapture`; программного промпта у AudioTee-категории нет). IPC `request-mic-access`/`open-privacy-settings` (target валидируется, non-darwin→true). macOS TCC программно НЕ выдаётся — кнопки только вызывают промпт/ведут в Настройки.

### Setup-стена (`95dd80d`)
Жёсткий гейт: пока НЕ (бэкенд установлен И whisper+vad в кеше) — full-cover overlay `#setupGate` (шаги «Установить бэкенд» → «Скачать модели» с прогрессом, реюз install/download-флоу), нормальный UI перекрыт. Готовность через `app-readiness` IPC: `backendAvailable()` + кеш whisper/vad проверяется **Node fs** (НЕ спавн python — до установки его нет), пути зеркалят backend.py `_model_cached` (coupling: менять оба). Диаризация НЕ в стене (опциональна, gated) — гейчится per-run в process-audio (diarize=true + нет pyannote → отказ). **Fail-closed**: `#setupGate` по умолчанию видим, скрывается только на confirmed-ready; ошибка IPC оставляет стену (не открывает приложение). Триггеры пере-проверки: boot / focus / после install-closed / после download-closed. Скачивание из стены scoped `["whisper","vad"]` (не тянет pyannote даже при токене). Тесты: path-exactness (падает при дрейфе путей), 4-state predicate, show/hide, fail-closed, diarize-гейт.

### Фиксы live-теста (`c39bd83`)
- **Двойной keychain-промпт** на старте: `loadToken` дёргал `safeStorage` дважды (get-presets + preflight). Фикс: module-кеш токена + `encryptionAvailable()`, `loadToken` short-circuit на `!fs.existsSync(SECRET_FILE)` (fresh без токена → keychain не трогается вообще). Один промпт при смене подписи (ad-hoc rebuild) — неизбежен, для стабильного .app «Always Allow» разово.
- **Прогресс скачивания моделей**: байт-прогресс % / МБ через `snapshot_download(tqdm_class=_ProgressTqdm)` (реальный `tqdm.auto.tqdm` subclass — HF юзает tqdm_class двуролево, дак-стаб падает на `set_lock`; эмит только для bytes-бара `unit=="B"`, throttle, emit в try/except, `_emit_lock` не путать с tqdm `_lock`). VAD (torch.hub) — грубо (мелкий). UI: %/МБ в стене + «Модели».
- **Отмена скачивания**: кнопки в стене + настройках → существующий cancel IPC. **Чистка частичной модели авторитетно на стороне родителя** (main.js close-handler после смерти дочернего: `cleanupPartialModelCache(inFlightModelId)` при canceled||code≠0) — гонки нет (worker-тредов уже нет), пути single-sourced из тех же хелперов что readiness → после отмены whisper-папки нет → `_model_cached`=false → стена НЕ снимается ложно. `inFlightModelId` = модель со `stage` без `stage_end` (завершённые не трогаются). Backend SIGTERM-cleanup остаётся best-effort.

### Фиксы live-теста #2 (2026-07-07)
- **Keychain-промпт каждый запуск даже без токена** (`5a87f55`): `isEncryptionAvailable()` дёргался на старте в `loadPresetsData` (для `secretEncrypted`). Фикс: gate `token ? encryptionAvailable() : false` на обоих return-путях — без `.secret` `safeStorage` не трогается вообще → ноль промптов. С токеном — один промпт/запуск (ad-hoc не закрепляет «Always Allow», неизбежно). Диагностика: на fresh не было ни `.secret` ни presets, промпт всё равно → значит `isEncryptionAvailable`, не `loadToken`.
- **Системный звук не писался в упакованном .app** (`dac47d6`, ПОДТВЕРЖДЕНО на рантайме): AudioTee спавнил бинарь по asar-относительному пути (`join(__dirname,"../bin/audiotee")` внутри app.asar), полагаясь на авто-редирект Electron в .unpacked. Диагностика: и dev-, и packaged-бинарь standalone дают реальный PCM (не тишина) → OS/бинарь/TCC ок, баг в интеграции. Фикс: явный `binaryPath` в `new AudioTee({binaryPath})` — packaged `resourcesPath/app.asar.unpacked/node_modules/audiotee/bin/audiotee`, dev `APP_DIR/...` (helper `resolveAudioTeeBin`, mainutil). Опция называется именно `binaryPath` (проверено в audiotee dist/index.js:84). Плюс `[audiotee]` console-логи (binaryPath + error/log events) для будущей диагностики.
- **Ссылка «Где взять токен?»** (`b710274`): у поля HF-токена подсказка + линк на huggingface.co/settings/tokens. Generic `open-external` IPC (https-only гейт — renderer не может открыть произвольную схему), `openExternal` в preload.
- **Трей-иконка не видна в упакованном .app** (`28566c1`, ПОДТВЕРЖДЕНО на рантайме): `createTray` грузил PNG через `nativeImage.createFromPath(APP_DIR/assets/...)` = asar-путь → пустая иконка → невидимый трей (тот же класс, что audiotee). Hidden Bar исключён (не запущен). Фикс: helper `resolveAssetPath` (mainutil) — packaged `resourcesPath/app.asar.unpacked/assets/trayTemplate.png`, dev `APP_DIR/assets/...`; `asarUnpack` добавлен `assets/trayTemplate*.png` (и @2x — retina-сиблинг тоже реальным файлом); `if(icon.isEmpty()) console.error` для диагностики. **Общий урок вынесен в G-правило** `g-electron-asar-path-audit` (general-rules.md, reflect-session): при упаковке Electron dev-first аудитить все `__dirname`/APP_DIR рантайм-пути ДО сборки.
- **safeStorage/keychain на ad-hoc**: `isEncryptionAvailable()` возвращает false в упакованном ad-hoc .app → токен хранится base64 (варнинг в UI, честный fallback). Не баг — корень тот же (нет стабильной keychain-идентичности без Developer ID). HF read-токен, локально, в `.secret` (не в git) — риск низкий. Владелец решил оставить (пункт 1).

## Релиз v1.0.0 (GitHub Release)
`gh release create v1.0.0` на `main` (`28566c1`), приватный репо → виден только владельцу. Вложения: `Meeting Recorder-1.0.0-arm64.dmg` + `.zip` (~98МБ). Заметки: требования (arm64, LM Studio, интернет на первый запуск), шаги установки (drag→Gatekeeper right-click Open→setup-стена→разрешения), ad-hoc-ограничения, sha256 dmg (`1f08a601…`). URL: https://github.com/ArtemiiF/MeetingRecorder/releases/tag/v1.0.0. Скачать: `gh release download v1.0.0 -R ArtemiiF/MeetingRecorder`. Следующая версия: bump `version` в package.json → пересборка → `gh release create vX.Y.Z`.

### Ручной smoke упаковки (НЕ проверено автоматом)
- Выдача TCC mic при первом запуске (системный звук — ПОДТВЕРЖДЁН рабочим после binaryPath-фикса на реальном прогоне 2026-07-07).
- Полный флоу «Установить бэкенд» на чистой машине (~1.3ГБ download).
- Gatekeeper: non-notarized .app даст «приложение повреждено/неизвестный разработчик» — правый клик → Открыть (снять quarantine), неизбежно без платного Developer ID.
- Нет иконки — ships с дефолтной Electron (follow-up).

## Фича: персист-очередь записей (`68aac2d`, ветка feat-pending-recordings)

Запись → стоп → снова запись, не обрабатывая → записи копятся и ждут (раньше single-slot `recordedFile` перезаписывался — вторая запись теряла первую из UI).

- **Единый источник правды** `state.pendingRecordings` — весь single-slot record-путь (`recordedFile`/`recordedMic`/`recordedSystem`/`recordedId`) удалён; `recorded`-хендлер ТОЛЬКО append + render (не трогает `state.processing`/`setProcessingUI`/log — иначе завершение записи рвало бы live-run соседней обработки, это был blocker критика, унифицировано и залочено тестом).
- **Персист через перезапуск**: аудио пишется сразу в `APP_DIR/recordings/<id>/` (постоянно, НЕ подметаемый TMP_DIR; кеш-резюме остаётся в TMP_DIR), манифест `recordings/pending.json` (writeJsonAtomic). На старте `list-pending-recordings` восстанавливает очередь и дропает записи с пропавшими файлами. IPC `remove-pending-recording` (delete dir + entry) для ✕ и после успешной обработки.
- **Обработка**: per-row ▶ / «Обработать все» через единственный слот procProc (mirror import-очереди); успех → remove из манифеста (уже в Истории), фейл → остаётся для retry.
- **Ревёрт гейта `0a79d98`** (по решению владельца): запись разрешена во время обработки — recBtn не disabled от `state.processing`, `toggleRecording` без early-return. Хардварный мьютекс (2 записи разом, main.js) + download×запись — на месте. id = `<displayStamp>-<rand4>` (dir и manifest id равны, коллизии по секунде исключены).
- Минор (TODO): sync reject-путь processAudio оставляет строку `running` при desync main↔renderer (low-reach).

## Что доехало до main (сессия 2026-07-06 вечер) — хвосты TODO

Четыре кода-чанка, каждый своей веткой через критик-гейт (все approve):

- **backend-минорки** (`7c8da45`): cap Whisper `initial_prompt` — `_build_initial_prompt()`: бюджет 224 токена (эвристика ceil(chars/3), без токенизатора), контекст никогда не режется, глоссарий дропается с хвоста списка (user-термины в голове выживают), контекст ≥ бюджета → глоссарий целиком в дроп; 8-бит WAV int8→uint8 + центрирование −128 (PCM-8 unsigned по спеке); autouse-фикстура стабит и `requests.get` (503). `_glossary_prompt` остался (жив только тестом) — см. TODO.
- **renderer-минорки** (`0a79d98`): suggest-эвикшн `slice(-CAP)` (новые выживают); комментарий glossaryDismissed; `toggleRecording` no-op на старт при `state.processing` (кнопка+tray), stop не блокируется; `paraInboxLoaded` после успешного fetch (упавший — ретрай при входе); PARA row-кнопки глобально disabled на «разобрать все» + re-enable недошедших при cancel.
- **main.js-минорки** (`365e6a1`): `download-models` отказывает при `recordProc || tee` («Дождитесь окончания записи»); reset-app: `writeJsonAtomic` ДО `saveToken("")` — disk-full больше не стирает токен без сброса пресетов. Обратный гейт (старт записи при активном скачивании) НЕ добавлен — продуктовое решение за владельцем (TODO).
- **батч retry per-element** (`cfcce85`): row-↻ на 🔴-строках очереди после батча; `retryQueueItem(idx)` реюзает один слот `startProcessing(false)` (resume, не fresh); флаг `queueSingleRetry` гасит авто-каскад `advanceQueue` (иначе перепрогон терминальных после ретрая середины); гейт `state.recording || state.processing`; рендер очереди переписан на createElement + closure-by-index (chip-паттерн, имена файлов не в атрибутах).

Минорки критиков этой пачки — TODO.md секция «2026-07-06, вечер». Что закрыто — TODO.md секция «Закрыто».

## Предыдущая сессия (2026-07-06 день) — словарь

Три чанка, каждый своей веткой через критик-гейт:

- **DEFAULT_GLOSSARY 49→106** (`8f93a7a`): распространённые IT/бизнес-термины (продукт/аналитика/dev/инфра/AI). Golden-дубликат в tests/renderer.test.js обновлён синхронно (tripwire сохранён). Хвост: 106 терминов не влезают в ~224-токенное окно Whisper initial_prompt — см. TODO.
- **Чипы-UX вкладки Словарь** (`cdf21f2`+`b28d276`): textarea → чип-лист (добавить input/Enter, удалить ×, счётчик «N терминов»), кнопка «Дополнить распространёнными» (case-insensitive мёрж дефолтов, текущий порядок первым), textarea остался скрытым «текстом»-тоглом (двусторонний sync). Критик-reject пойман и исправлен: `data-term`-атрибут с user-строкой (escapeHtml не экранирует `"` → attribute-injection + сломанное удаление) → слушатели через closure-by-index, атрибут удалён; regression-тест с кавычкой в термине. Паттерн обязателен для всех новых чипов: никаких user/LLM-строк в HTML-атрибутах.
- **Авто-пополнение «Предложения»** (`ef42c2d`): новый pipeline-stage `suggest` после summary (всегда, независимо от do_summary; LM недоступен → `[]` + warn, не 🔴); LLM-извлечение кандидатов из транскрипта с жёсткими код-гейтами (substring по транскрипту против инвенций, дедуп через `_term_or_declined_form`, len>2, cap 20); кеш `suggest-{lang}{glossary-suffix}.json` + input_hash транскрипта; suggestions едут в `done`-payload (main.js не менялся — форвардит verbatim). Renderer: presets-поля `glossarySuggestions` (pending, cap 100) / `glossaryDismissed` (никогда не пере-предлагать), секция «Предложения» с ✚/✕/«Принять все». Autouse-фикстура в pytest стабит requests→503 (LM Studio на машине живой — полнопайплайновые тесты больше не бьют в него).

- **Tray в menu bar** (`adf9726`): иконка (template PNG assets/, авто dark/light) с меню «Начать/Остановить запись» (динамический лейбл) / «Открыть Meeting Recorder» / «Выйти»; `tray.setTitle("REC")` при записи. Tray-toggle реюзает renderer-флоу кнопки записи через IPC (`toggleRecording` извлечён, логика записи в main не дублируется); все 3 сайта `state.recording` пушат `recording-state`. Закрытие окна → hide в tray (запись переживает, `recordProc`/`tee` module-level в main), настоящий выход — «Выйти»/Cmd+Q (`before-quit`-флаг). Dock остаётся. Иконка попиксельно не проверялась — первый запуск = smoke (внешний вид, template-tint, REC-тайтл, старт из tray при скрытом окне).

Минорки критика этой сессии — в TODO.md (секция 2026-07-06).

## Push-гейт из чужого cwd (важно для следующих сессий)

Сессия шла из `/Users/filanovskii/SupersetWorkspace` — PreToolUse-hook `auto-critic.sh` исполняется в cwd сессии, там нет `origin` → precheck fail-closed ДО чтения маркера (bypass-строка не помогает). Владелец разрешил обход: временный ref `git -C <session-cwd> update-ref refs/remotes/origin/main HEAD` → marker с `bypass: owner-accepted-risk` → push → `update-ref -d`. Правильный фикс — запускать сессию из каталога репо; harness-gap хука (не резолвит репо из команды) не чинился.

## Предыдущая сессия (2026-07-03, `main` был `75414d0`)

Одиннадцать чанков за день, каждый своей веткой через критик-гейт. Утро (см. git log): 4 минорки критика (`d8c33c8`+`554b4b1`), mix offset xcorr (`468ade2`), auto-«Я» из mic-дорожки (`385ae7d`), секция «Модели» в настройках (`c43c892`) + UX-правка кнопок «Проверить»/«Скачать недостающие» (`23437be`).

Вечер — вкладка Словарь + продиктованный бэклог владельца (митинг-заметка meeting-2026-07-03-123128):

- **📖 Словарь — 4-я вкладка** (`c43af24`): глоссарий уехал из карточки Запись в свой top-level таб; `DEFAULT_GLOSSARY` (48 IT-терминов) подставляется при пустом словаре (in-memory, паттерн authorName; пустой = «дай дефолты» — решение владельца «заполним распространёнными»). Дубликат списка в тесте — golden value, править оба.
- **Настройки-консолидация** (`72bb84f`): HF-токен, authorName и outDir («куда сохранять») уехали в settings overlay (секции «Личные данные», «Куда сохранять»). outDir авто-следует за vault (`<vault>/Meetings`) при создании vault, если не задан вручную (`outDirCustom`); ручной выбор всегда побеждает; без vault — прежний дефолт. Старые заметки не переносятся.
- **Батч-импорт + индикатор записи** (`6af8fd0`): multi-select файлов → последовательная очередь (renderer-side, procProc один слот; статусы ⏳/🔵/🟢/🔴/⏹; фейл элемента → продолжаем, cancel → стоп всего батча; re-pick заменяет очередь); reprocessHistory = очередь-из-1. Пульсирующая красная точка на кнопке 🎙 в topnav — видна с любой вкладки, все 3 сайта state.recording (start/stop/mic-error).
- **История-фиксы** (`439d25e`): сортировка по `stamp` (время записи; mtime телепортировал старые заметки наверх после rename спикеров), кап limit=200 снят (все заметки); календарь-фильтр был логически корректен — исправлены anchored-причины «не работает»: `color-scheme: dark` + `input[type=date]` в тему (пикеры были светлые); поиск словоформ — `ruStem` суффикс-стриппинг (проблема↔проблемы, миграция↔миграций; англ. термины не затронуты), без новых deps; «авто» удалён из языков записи (коэрция stored "auto"→"ru"), в фильтре истории «авто» остался для старых заметок.
- **PARA-UX** (`0022d5b`): спиннер на разбираемой строке при «разобрать все»; root cause стирания разобранных — renderParaInboxView перезапрашивал inbox при каждом входе → `paraInboxLoaded`-флаг (fetch один раз; сброс при vault create/reset); разобранные серые+disabled и живут до «Обновить»/перезапуска; кнопка «Обновить» (⟳, disabled мид-батч); ручное «Разложить» тоже серит (не удаляет); дерево Хранилища collapsed по умолчанию.
- **Сброс приложения** (`75414d0`): кнопка «Сбросить и настроить заново» (danger, секция «Сброс» в настройках) — confirm → ПИШЕТ свежий presets.json из example с `para.root=""` (НЕ unlink — критик-reject поймал воскрешение root из example-fallback; writeJsonAtomic) + saveToken(""), busy-guard по 4 процессам; заметки/записи/index.db не трогаются; после сброса init() + PARA re-render.

## Тесты

`npm test` = JS `node --test` (287/287) + PY pytest (238/238). Всё замокано — живой e2e (RAG, коррекция, auto-«Я», скачивание моделей, батч-импорт + row-retry, reset, suggest-стадия, tray, персист-очередь записей + запись-во-время-обработки) не гонялся; первый реальный запуск = smoke.

## Решения владельца (действуют)

- 2026-06-30: live-транскрипт не нужен; календарь-интеграция выкинута; live smoke пропущен; невалидная PARA-категория → error; reasoning_content в заметку — никогда.
- 2026-07-03 (митинг-заметка): шаблоны и языки остаются; «авто» из языков убрать; хранилище свёрнуто по умолчанию; пустой словарь = заполнить распространёнными терминами.

## Хвосты (TODO.md — источник правды)

- Осталось открытым (полный список TODO.md): VAD-clip auto-«Я» при nonzero delay (+интеграционный тест при калибровке); ASSOC-допущения model-download; ruStem на 1-3-буквенных запросах; renderRail O(n) без виртуализации; chat degraded-бейдж string-match; свежие минорки критиков (осиротевший `_glossary_prompt`, тихий catch PARA-inbox, re-enable не в finally, PARA reverse race, батч retry↔cancel краевые, обратный гейт download→запись — продуктовое решение).
- Калибровка HYPO-порогов auto-«Я» и xcorr по первому реальному прогону (лог пишет per-label mic_ratio/duration).

## Git / процесс

- Remote `ArtemiiF/MeetingRecorder` (private, solo). `main` = origin/main = `fa7c85e` (категории словаря, PR #13). Тег `v1.0.0` = релиз с .dmg (перенесён в публичный репо, sha переписан). Фича-ветки спрюнены.
- УСТАРЕЛО (репо публичный, см. шапку): push в main агенту запрещён, только PR. Критик-гейт остаётся перед push ветки. Механика маркера: критик по `git diff origin/main...HEAD` → marker `printf 'verdict: approve\ndiff-sha256: %s\n'` где sha = `printf '%s' "$(git diff origin/main...HEAD)" | sha256sum` (именно `printf '%s'` — прямой pipe оставляет \n → mismatch) → push отдельной командой. Marker одноразовый, TTL 300 c. Marker и push НЕ объединять в одну команду (hook проверяет до выполнения).
- Agent-report'ы (`.claude/agent-reports/`) — untracked, в коммиты не включать.
- Фиксы предсуществующих поломок — отдельным чанком от фичи.

## Окружение

- macOS: `timeout` НЕТ (только `gtimeout`); pytest через `../venv/bin/python -m pytest tests/ -q`; venv: numpy 2.4.4, scipy 1.17.1. Bare `grep` в этой среде мангуется rtk-хуком — субагентам использовать `rtk proxy grep`.
- LM Studio `:1234`: reasoning-модель (max_tokens ≥1500-2500) + embedding-модель для вектора; без неё keyword-only + warn.
- Preflight-панель: LM Studio / разрешения / ffmpeg / модель / HF-токен / embedding. Секция «Модели»: статус кеша + докачка (MLX Whisper ~1.5 ГБ, Silero VAD, pyannote 3.1 ×3).
